## Change Packet

- **Task ID / branch:** IMP-XXX / `imp/IMP-XXX-<slug>`
- **Spec basis:** <!-- packet specs/adrs this PR implements; link the spec sections -->
- **Consumer search:** <!-- every spec/consumer grepped for this contract's symbols; list files checked -->
- **Contract delta:** <!-- proto/migrations/schema/wire changes, or "none" -->
- **Owned paths changed:** <!-- must be a subset of the packet's owned_paths -->
- **Tests executed + results:** <!-- commands run and their results -->
- **Codegen drift:** <!-- `0` or describe; codegen must be byte-identical -->
- **Evidence manifest / ci_run_id / run_attempt / source_tree_hash:** <!-- for -done PRs: evidence/IMP-XXX/manifest.json details -->
- **Cleanup verification:** <!-- git status clean after codegen + verify; no verification-created drift -->
